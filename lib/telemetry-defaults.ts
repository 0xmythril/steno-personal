// The PostHog project this build reports to. The token is the write-only
// key PostHog expects every client to embed — not a secret — and it ships
// here so a fresh instance reports from its first day. Empty means no build
// has been pointed at a project yet, and nothing is sent.
export const POSTHOG_DEFAULT_KEY = ''
export const POSTHOG_DEFAULT_HOST = 'https://us.i.posthog.com'
