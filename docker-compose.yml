services:
  app:
    build: .
    ports: ["127.0.0.1:3000:3000"]
    environment:
      DATA_DIR: /data
      # SECRET_KEY: set one, or let the container generate it into the volume
    volumes:
      - data:/data
    restart: unless-stopped
volumes:
  data:
